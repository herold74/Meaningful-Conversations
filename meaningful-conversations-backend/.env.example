# -----------------------------------------------------------------------------
# EXAMPLE ENVIRONMENT CONFIGURATION
#
# Create a copy of this file named ".env" in the same directory
# and fill in the values for your local development environment.
# -----------------------------------------------------------------------------

# --- Application Environment ---
# Set to 'development', 'staging', or 'production'.
# In 'development' or 'staging', potentially destructive database schema changes
# can be applied automatically for faster iteration. 'production' is protected.
ENVIRONMENT_TYPE=development

# --- Database Connection (for local development) ---
# Use the format for your MySQL setup. See the backend README for details
# on connecting from a container vs. running directly on your machine.
# Example: mysql://USER:PASSWORD@HOST:PORT/DATABASE_NAME
DATABASE_URL="mysql://root:password@localhost:3306/meaningful_convos_db"

# --- Gemini API Key ---
# Get your key from Google AI Studio: https://aistudio.google.com/app/apikey
API_KEY="YOUR_GEMINI_API_KEY"

# --- JWT Secret for Authentication ---
# A long, random string used to sign user session tokens.
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="YOUR_RANDOMLY_GENERATED_JWT_SECRET"

# --- Server Port ---
# The port the backend server will run on for local development.
PORT=3001

# --- Mailjet Email Service (for password resets, etc.) ---
# CRITICAL: The sender email must be a verified sender in your Mailjet account.
MAILJET_API_KEY="YOUR_MAILJET_PUBLIC_KEY"
MAILJET_SECRET_KEY="YOUR_MAILJET_SECRET_KEY"
MAILJET_SENDER_EMAIL="YOUR_VERIFIED_SENDER_EMAIL"

# --- Application URLs ---
# The URL where your frontend is running.
FRONTEND_URL=http://localhost:3000

# --- Initial Admin User (for Database Seeding on first run) ---
# The server will create this user with admin privileges on startup if it doesn't exist.
INITIAL_ADMIN_EMAIL=admin@example.com
INITIAL_ADMIN_PASSWORD=yoursecurepassword
