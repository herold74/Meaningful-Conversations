# iOS/Xcode staging backend — bake into Capacitor build
# Usage: npm run sync:ios-staging
# Or: export VITE_CAPACITOR_BACKEND=staging && npm run build && npx cap sync ios
# Revert to production API: npm run build && npx cap sync ios (omit this variable)

VITE_CAPACITOR_BACKEND=staging
