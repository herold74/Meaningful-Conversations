# Work-in-progress screenshots

Temporary captures during manual or agent work. **Gitignored.**

Move finished assets into `journey/` or regenerate `app-store/vX.Y.Z/` via `scripts/prepare-asc-screenshots-from-assets.py`.

Do not upload files from this folder to App Store Connect.
